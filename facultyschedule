CREATE ALGORITHM=UNDEFINED DEFINER=`root`@`localhost` SQL SECURITY DEFINER VIEW `faculty_course_taught` AS select `course_participant`.`UNI` AS `uni`,`sections`.`course_id` AS `course_id`,`sections`.`year` AS `year`,`sections`.`semester` AS `semester` from (`course_participant` join `sections` on((`course_participant`.`section_call_no` = `sections`.`call_no`))) where ((`course_participant`.`role` = 'F') and (`course_participant`.`taught_or_taken` = 1))
